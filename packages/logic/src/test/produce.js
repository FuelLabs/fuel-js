const { test, utils } = require('@fuel-js/environment');
const genesis = require('../produce');

module.exports = test('produce', async t => {
  try {

  } catch (testError) { console.error(testError); }
});
