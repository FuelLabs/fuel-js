{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm run clone && node config/build",
    "clone": "rm -rf fuel && git clone git@github.com:FuelLabs/fuel.git && cd fuel && npm install && npm run build && cd .."
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "write": "^2.0.0"
  },
  "dependencies": {
    "pubnub": "^4.28.4"
  }
}
